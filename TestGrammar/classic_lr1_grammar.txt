# 经典LR(1)测试文法
# 特点：需要LR(1)前瞻能力的标准测试案例，展示指针赋值语义
# 复杂度：中等
# 分析器兼容性：SLR(1)有冲突，LR(1)成功
# 典型输入示例：
#   - 指针赋值: * id = id
#   - 间接访问: id = * id
#   - 复合操作: * * id = id

S -> L = R
S -> R
L -> * R
L -> id
R -> L
